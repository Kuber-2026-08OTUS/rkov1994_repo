apiVersion: v1
kind: Service

metadata:
  name: homework-service
  namespace: homework

spec:
  type: ClusterIP

  selector:
    app: homework

  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
